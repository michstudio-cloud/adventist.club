[pytest]
testpaths = tests
asyncio_mode = auto
# One event loop for the whole run: the app's asyncpg pool is bound to a loop.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
